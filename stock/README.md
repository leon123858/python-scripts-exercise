# Stock

Python exercises for fetching Taiwan stock data, analyzing simple candlestick and RSI events, and drawing charts.

## Setup

```powershell
make sync
```

## Common Commands

```powershell
make test-local
make test-integration
make format
make lint
make typecheck
make check
make run
```

`make test-local` avoids tests that require live `twstock` network access.
